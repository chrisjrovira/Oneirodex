# Optional extras — installed on purpose, never by the base image.
#
# INSP-42 (v11 H4a): unofficial store clients for the opt-in ownership
# registers. Register-only (IDs + names); Oneirodex never downloads a title.
# Each is used only when ENABLE_UNOFFICIAL_STORE_SYNC names its store; without
# the package the plugin reads "available" and the sync button says which
# package to install. CSV import needs neither.
xbox-webapi>=2.0
psnawp>=2.0
