{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "social",
  "description": "Least-privilege Friends companion webview — site browse only, no install/launch FS ACL",
  "windows": ["social"],
  "permissions": [
    "core:default"
  ]
}
