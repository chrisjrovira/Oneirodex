{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "thin-library",
  "description": "Thin library webview — site browse only",
  "windows": ["library"],
  "permissions": ["core:default"]
}
