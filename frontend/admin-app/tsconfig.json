{
  // admin-app SPA — files renamed to .ts/.tsx in PR-4 (e), annotated for
  // strict in PR-4 (b). Local override only — tsconfig.base.json stays
  // strict:false for the other workspaces until each does its own pass.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "strict": true },
  "include": ["src"]
}
