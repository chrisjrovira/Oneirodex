{
  // admin-app SPA — files renamed to .ts/.tsx in PR-4 (e). The `strict: true`
  // override is deferred: the annotation pass that makes `tsc --noEmit` clean
  // under strict is tracked as follow-up. Until then this inherits the shared
  // on-ramp base (strict:false) so `build` / `typecheck` stay green.
  "extends": "../../tsconfig.base.json",
  "include": ["src"]
}
