{
  // `@oneirodex/ui` — the shared infrastructure package (TypeScript since
  // Phase 3.3); every SPA consumes these types.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "jsx": "react-jsx",
    "types": ["vitest/globals", "@testing-library/jest-dom"]
  },
  "include": ["src"]
}
