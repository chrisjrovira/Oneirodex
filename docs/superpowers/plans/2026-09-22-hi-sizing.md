# Phase H-I — the rest of the inventory, sized

**Measured 2026-09-22 on `main`**, after H1 (merged + deployed) and with H2 / H3 / H4 in review. This is the sizing pass the v11 plan asked for before H-I builds anything: one row per remaining item, what it actually needs, what it waits on, and where it goes. Sizes are **S** (one sitting, one file or two), **M** (a slice: model or module + UI + tests), **L** (a phase's worth, or a decision before code).

Nothing here is scheduled by writing it down. The order at the bottom is the proposal; his interrupts outrank it.

## What the harvest already closed

The register rows below are **done** and are not in the sizing: INSP-31 (hash identify, H1a), INSP-34 (TOSEC/MAME, H1b), INSP-5 (1G1R, H1c), INSP-24 (DAT repair preview, H1f), INSP-35 (anti-cheat, H1e), INSP-36/22/37/38/39 (mods, H2a–e), INSP-40/41 (XR, H3a–b), INSP-42/43/44/6/1/45 (stores, arcade, GPU, notify, saves, assists — H4a–f). INSP-7 and INSP-33 are **documented as deferred** with their triggers (`docs/admin/libraries-and-scans.md` › *Identify sources deferred*).

Also settled while measuring: **ES-DE and Pegasus export both ship** (`GET /api/export/esde`, `/api/export/pegasus`, `routes_apis/wave8_11.py`; user docs describe the Systems *Export packs* note). The "two-doc status conflict" the plan flagged was stale — what is actually open there is INSP-9, a *third* frontend (Steam shortcuts), which is its own row below.

## Sizing

| Item | What it is | Size | Depends on | Slot |
|---|---|---|---|---|
| **INSP-3** | Nested AND/OR filter builder + **named saved filters**, reusable by Discover shelves. A real query model (JSON tree) in `browse_filters`, a builder UI, and persistence on the user. The flat chip row stays as the simple face. | **L** | — | **First.** Two rows below fall out of it. |
| **INSP-29** | Smart collections = a saved filter with a name and a tile. | S | INSP-3 | Same phase, right after. |
| **INSP-4** | Persistent **not interested** on Discover + wishlist: a negative signal the recommender keeps. | M | — | With INSP-3 (both touch Discover queries). |
| **INSP-4b** | Taste picker / *surprise me* over `curated_for_you`. | S | INSP-4 | After INSP-4. |
| **INSP-21** | Fifth play status **Won't play** — one enum value, one migration, filters and tiles. Doubles as the INSP-4 signal if we want one enum rather than two stores. | S | Decide with INSP-4 | Fold into the INSP-4 slice; do not build twice. |
| **INSP-12** | **Short-code / QR device pairing** for API tokens: `POST /api/tokens/pair` mints a short code, `/pair` renders it, the device redeems it. Promoted in the register (three independent sightings). | M | — | **Second slice.** Nothing waits on it, and it unblocks INSP-27. |
| **INSP-27** | Handheld **CFW sync client** (muOS / KNULLI / NextUI class). | **L** | INSP-12, INSP-1 | Not in H-I. Its own program if he wants it. |
| **INSP-18** | Physical copy fields — condition, CIB, paid price, notes, on the copy rather than the game. | M | — | Collector row; schedule when he asks. |
| **INSP-19** | Play **heatmap** + session notes. `PlaySession` rows already exist; Statistics has totals, not a habit calendar. | M | Real play data | Cheap once people play — after a month of sessions on the box. |
| **INSP-26** | Public list pages + RSS/JSON. | M | **A deliberate auth story** (token or household-signed feed) | Blocked on a decision, not on code. Ask before building. |
| **INSP-9** | **Steam shortcuts export** (artwork + launch target) beside ES-DE / Pegasus. | M | — | Good H-I candidate; the two existing exporters are the pattern. |
| **Meta Quest identify** | `providers/meta_quest.py` does *images*; identify **search** is the gap, so a Quest shelf matches by name only. | M | — | With the next identify pass. |
| **Epic / itch / Amazon identify search** | Same shape: these are ownership sources, not identify sources. One `SourceSpec` each. | M each | — | One slice for all three, PC order only. |
| **SRV-3** | Game-server **Docker control** (start/stop) behind an explicit flag. | M | An operator decision about the socket | Flagged off by default; ask before shipping the flag. |
| **SRV-4** | Party **Join server** deep link. | S | SRV-1/2 (shipped) | Any time. |
| **GOW-3** | Party invite carries the remote-play PIN when the host is online. | S | GOW-1/2 (shipped) | Any time. |
| **GOW-4** | Compose profile docs for a Wolf sidecar (operator-owned GPU). | S (docs) | — | Rides with the next ops docs pass. |
| **LIGHT-3** | Admin UI + Big Picture toggle for *sync lights*, member preference. | S | LIGHT-1/2 (shipped) | Any time. |
| **LIGHT-4** | Cover-colour sampling for ambient light. | S | LIGHT-3 | After LIGHT-3. |
| **ART-4** | Ops: disk quota for generated art + purge orphans. | S | — | With the next Ops slice. |
| **W25-ZONES** | Roku-like ambient zones · screensaver gaming city. | **L** | Art direction (his) | Creative call first — not a code slice. |
| **Admin Jinja tail** | **29 of 45** admin templates still render server-side (`admin_render` defaults to `auto`); the census in `docs/dev/admin-jinja-inventory.md` is Tier 1 → 3 ordered. | **L**, but divisible | — | One port per slice **between** other slices, as H-D.5 established. Refresh the census counts when the next port lands. |
| **INSP-SCAN-GODFN** | The two scan god-functions (`scan_identify` walk, `scan.py` orchestration) are still long after H-D.4's pure moves. | **L** | A real test net first | Only with the scan suites as the net; never alongside a behaviour change. |
| **Pass-8: DB engine + cache** | Postgres + SQLite (± MariaDB), bundled Redis on or opt-in. Proposal written: `docs/strategy/db-engine-and-cache-2026-09.md`. | **L** | **His answer** (Human Queue `h8-db-engine`) | Blocked on the gate. |
| **Pass-8: history page** | Reference-only console/release timeline. Proposal: `docs/strategy/game-history-timeline.md`. | **L** | **His answer** (scope + placement, `h8-history-page`) | Blocked on the gate. |
| **Pass-8: scene version signal** | Freshness-only cross-check, never a source. | M | **His ruling** (`h8-scene-signal`) | Blocked on the gate. |
| **INSP-7 / INSP-33** | Community ROM metadata (Class D) · offline hash index. | M each | Their written triggers | Deferred on purpose; documented. |
| **Native rewrites** (RTC-N, GPU-N, MAL-N, NCS-N, MOD-N) | Parked in the v1 cycle. | **L** each | — | Stay parked. |

## Proposed order for H-I

1. **INSP-3** filter model + builder (L) → **INSP-29** smart collections (S).
2. **INSP-4** not-interested, with **INSP-21** folded in as the same enum (M+S) → **INSP-4b** (S).
3. **INSP-12** QR / short-code pairing (M).
4. A **small-wins slice**: SRV-4, GOW-3, LIGHT-3, ART-4 (four S rows, one PR each, one afternoon).
5. **INSP-9** Steam shortcuts export (M).
6. Identify gaps: Meta Quest search, then Epic / itch / Amazon (M).
7. Admin Jinja ports continue one per slice throughout, Tier 1 first.

Everything gated on him (DB engine, history page, scene signal, INSP-26 auth story, SRV-3 socket) stays on the Human Queue and is built only after an answer.

## Related

- Board: [2026-09-18-v11-cycle.md](2026-09-18-v11-cycle.md)
- Register: [capability-inspiration.md](../../strategy/capability-inspiration.md) · [capability-harvest-2026-09.md](../../strategy/capability-harvest-2026-09.md)
- Admin tail census: [admin-jinja-inventory.md](../../dev/admin-jinja-inventory.md)
