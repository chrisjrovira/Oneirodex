# v1 cycle — review, tail, debt, features, 1.0.0

**Cut:** 2026-09-16 · **Base:** `main` @ `763a6546` · **Ends at:** tagged `1.0.0`

Board for the cycle that follows the modernization program (PRs #33, #61, #69–#76)
and the Cursor run that landed while it was paused (PRs #77–#112 — 427 files,
+11,117 / −5,126, none of it reviewed from this side).

One branch per phase, cut from `main`, merged back on green CI. Not a long-lived
integration branch: `chore/modernization` ended 149 commits behind and was
abandoned, while the stacked-PR half of the same program merged clean.

## Standing decisions (2026-09-16, human)

| # | Decision |
|---|---|
| D1 | Goal is the whole arc, ending in a **1.0.0 tag**. |
| D2 | **Merge and deploy authority delegated** — merge a phase PR on fully green CI; rebuild Unraid + Reset Themes at phase boundaries without asking. |
| D3 | **Human QA batched per phase** — refresh the Human Queue artifact at each phase end; work continues meanwhile. |
| D4 | **Feature tracks:** themes AI redesign · emulation (audio, achievements, EmulatorJS) · platform reach (thin client, Quest VR). Discover polish stays in the debt phase as UID-020. |

Unilateral calls (flagged per PR, one word vetoes): track `CLAUDE.md` · Quest VR is
WebXR/PWA before any APK · `updateschema.py` stays frozen, not deleted · doc-drift
resolves in favour of the newer register.

## Phases

| # | Phase | Branch | Status |
|---|---|---|---|
| 0 | Clean the workspace | `v1/phase-1-review` | **done** 2026-09-16 |
| 1 | Review PRs #77–#112 + re-baseline the suite | `v1/phase-1-review` · `v1/phase-1-rebaseline` | **done** — PR #113, #114 merged; deployed; QA batch 1 published |
| 2 | Close the modernization tail | `v1/tail-*` | in progress — #115 #117 #118 #119 #120 merged, #121 #122 in flight |
| 3 | UI debt + human-QA backlog | `v1/discover-*` | in progress — #126, #127 merged; Grid/top-bar needs his description |
| 4 | Emulation track | `v1/emulatorjs-engine` · `v1/member-play-engine` | in progress — #125 #128 merged (clock fixes, engine B); member engine choice this PR; RetroAchievements blocked on his API key; save-state layer next |
| 5 | Themes AI redesign | `v1/themes` | not started |
| 6 | Platform reach | `v1/platform` | not started |
| 7 | Cut 1.0.0 | `v1/release` | not started |

## Phase 0 — clean the workspace · done

- [x] `v1/phase-1-review` cut from `main` @ `763a6546`
- [x] `chore/modz-member` (39 commits, superseded by PR #77) tagged `archive/modz-member` @ `ffb3c224`, then deleted
- [x] Deleted `cz/cool-jemison-c1b749`, `cz/vigilant-gates-a00189`, `chore/modernization`, `chore/modz-primitives` (all 0 ahead of `main`)
- [x] Pruned all 5 stale worktrees — `git worktree list` is one entry again
- [ ] **Deferred, needs a permission rule:** drop scratch DBs `alembic_scratch_test` / `ci2_alembic` (`docker exec` blocked); delete the 3 merged `origin/cursor/*` branches (`git push --delete` blocked). Both cosmetic.

## Phase 1 — review of PRs #77–#112 · findings

Reviewed by risk area, not file-by-file. **fixed** landed in this phase's PR; **ticket** is
tracked for the phase named; **verified** means looked for, not found, with the evidence.

| # | Area | Finding | Disposition |
|---|---|---|---|
| F1 | — | ~~`gen_loading_motifs.py` emits untyped JS into a `.ts` file~~ | **Withdrawn.** `--check` exits 0 and `tests/test_loading_motifs.py` gates it. |
| F2 | — | ~~CSS-token ratchet went dark after the `.tsx` rename~~ | **Withdrawn.** `main` already walks `.tsx`/`.ts` (`d555258d`, PR #77); a `#ff00ff` probe in a `.tsx` under a scan root is flagged, exit 1. |
| F3 | #77 | `SYSTEM_MOTIF_FAMILIES` emitted as `Record<string, any[]>` in a strict package (`scripts/gen_loading_motifs.py:166`). | **ticket → Phase 2** (fold into F10). |
| F4 | #112 | **Unmatched list silently lost its name-transform trail.** `_unmatched_list_row` defaulted `include_transforms=False`; the only consumer (`admin_manage_scanjobs.js:151,1735-1749`) never opts in, so the "Name transform trail" expander rendered for nothing. Export and dupe-glance still passed `True`, which is why tests stayed green. | **fixed** — default on, `include=none` / `transforms=0` opt out; list is paginated so the CPU is bounded. Regression test `test_unmatched_list_carries_the_transform_trail_by_default`. |
| F5 | #112 | **Cron schedules evaluate in UTC and nothing said so.** `0 3 * * *` from America/Chicago is a 9–10pm local scan. | **fixed** — field is "Cron expression (UTC)" with a description; presets were relative and never had this problem. |
| F6 | #112 | **Day-of-month and day-of-week were ANDed; crontab ORs them** when both are restricted. `0 3 1 * 1` fired ~once a year instead of ~5×/month. The PR declared this out of scope; it is still wrong under a field labelled "Cron expression". | **fixed** — POSIX union in `next_cron_fire`; test `test_cron_day_fields_follow_posix_union`. Flagged in the PR body in case the out-of-scope call was deliberate. |
| F7 | #112 | `validate_cron_expression` walked ~1.15M minutes for a parseable-but-impossible expression (`0 0 30 2 *`), synchronously in the form POST. | **fixed** — day-stepping while the date mismatches; 0.001s now; test asserts `< 1s`. |
| F8 | #112 | `normalize_schedule_fields` fell through to a silent `once` job with `error: None` for an unknown choice — cron/interval failed closed, this failed open. | **fixed** + test. |
| F9 | #112 | Dead code presented as safety: `hasattr(job, 'schedule_kind')` guards (always true on a mapped model) and a `cursor.day <= monthrange(...)` check that cannot be false. | **fixed** — removed, comments say why. |
| F10 | #77 | **Member-app strict TypeScript is nominal.** `: any` annotations: **878 across 127 files** (top: `LibraryApp.tsx` 39, `GameDetailsPage.tsx` 34, `TrailersPage.tsx` 31, `ChatPanel.tsx` 28). Admin-app, converted by the earlier program, has **36**. `@typescript-eslint/no-explicit-any` is `'off'` in `eslint.config.js:110,148` — the flip passed by disabling the rule that would have failed it. | **ticket → Phase 2.** Turn the rule to `warn`, add an `any` ratchet with a per-file baseline (the `print_lint` model), work it down file by file. |
| F11 | #81–#97 | `extra='forbid'` on every pydantic model is strictly narrower than the hand-rolled guards it replaced. | **verified, no break.** Extracted every `JSON.stringify` body in theme JS (the volume-versioned surface that cannot update with the image) and checked each against its model; SPA API layers build explicit bodies (no spreads); api-client/desktop `lifecycle` posts `{records}` only. Models document the theme-JS edge cases (`image_id` untyped, `client` optional). Residual: it is a hard compatibility stance for an app with an installed companion binary — record it in the pydantic ADR (Phase 2). |
| F12 | #79/#80 | Transport swap: `credentials`, CSRF on mutating verbs, 401 → `/login`. | **verified.** Guarded (`redirecting` flag, same-origin, `/api` paths only); the requester's `onUnauthorized` double-fires the same navigation — idempotent. |
| F13 | #103 | Slow boot regressing into no `retroarch.cfg` write? | **verified, no.** `waitForMeasuredRefresh` resolves `null` at the deadline and the write proceeds as before; `afterStart()` now re-applies a late sample. **The reporter's actual complaint is audio sample rate (44.1 kHz device), not display Hz — Phase 4.** |
| F14 | #104 | CheapShark: `None` (transport/parse failure) vs `[]` (honest empty) keeps the last good snapshot. | **verified.** |
| F15 | env | Docker Desktop held **no containers**; `pydantic-core` had drifted to 2.49.0 again (second time — memory records the first on 2026-09-10). Every DB test hung on a refused connection. | **fixed** — `oneirodex-review-db` recreated from `docker-compose.review.yml`, `oneirodextest` created (fresh, so the local `alembic check` drift is gone by construction); `pydantic-core==2.46.5` now pinned in `requirements.txt` so `pip install -r` reasserts it. |
| F16 | #99 | `.gitignore` now un-ignores `.cursor/{environment.json,install.sh,start.sh}` — the "agent kit is local-only" policy already has a tracked exception. **Both** `CLAUDE.md` and `AGENTS.md` are untracked (the plan wrongly said AGENTS.md was tracked). | **ticket → Phase 2** (the unilateral "track CLAUDE.md" call covers both). |

**Method note.** F1/F2 came from reading an abandoned worktree's uncommitted diff, which is
against *its* base (`880bfcf7`), not `main`. Both were real when the branch was cut and both
were fixed upstream in better forms. Diff a salvaged change against `main` before believing it.

**Scoped tests run green after the fixes:** `test_scan_schedule_helpers` 13 · `test_utils_duplicate_check` 7 ·
`test_unmatched_suggested_kind` 14 · `test_scan_jobs_failure_reason` 9 · `test_routes_apis_scan` 35 ·
`test_unmatched_bad_match` 7. Ratchets: api-envelope 11 · print 591 · get_json 103 · css-token 0.

## Phase 1 — closed 2026-09-16

- **PR #113** — 16 findings triaged (see table above); six fixes in the scan-schedule/unmatched redesign.
- **PR #114** — the full suite named: **42 failed / 4,450 passed** (48 min), every ID grouped by cause in `docs/dev/test-suite-failures-2026-09-16.md`; three stale assertions fixed; print ratchet 592 → **591**. None of the 42 failing files are in the CI hand list.
- **Deployed** — `main` @ `10aec59e` built at 15:19 and Reset Themes run; `/awake` ok. This is the first deploy carrying PRs #111/#112.
- **QA batch 1 published** — https://claude.ai/artifact/U7qhn9GWpm1TzTPdxJjRtP (8 items: 3 live-box checks, 2 emulation facts, 3 calls to confirm or overturn).
- **Found while deploying, not ours to fix:** Authentik's worker has restarted **2,652 times** — it resolves a host named `redis` that does not exist on that box (only `oneirodex-challenge-redis`), so the proxy 502s and SSO cannot complete. Local admin login unaffected. Diagnosed and handed back; it predates this deploy.

## Phase 2 — in progress (2026-09-17)

**Decisions from QA batch 1, answered in chat:** admin scans redesign *looks right* · Deep discounts *has tiles* · cron crontab semantics *keep* · Quest *PWA first* · audio device is **48 kHz** · the refresh line is *missing entirely* on his machine · SSO: he brought `redis` up; verified healthy, OIDC start redirects to Authentik (his one sign-in still closes it) · **agent-harness files stay untracked, whatever agent** (D5).

| Slice | Status |
|---|---|
| ADRs 0006–0010 + index, `ci-gates.md` replaces `ci-wishlist.md` | **PR #115 merged** |
| D5: `CLAUDE.md`/`AGENTS.md` back out of git, `tests/test_repo_hygiene.py` guards it in CI, conventions moved to `CONTRIBUTING.md` | **PR #117 merged** |
| `any` ratchet (`scripts/any_lint.mjs`) + marker-set timing job | **PR #118 merged** — member-app 1,041 / admin 36 / others 0 |
| pydantic: `scan.py`'s 11 sites read against callers — **not wrap candidates**, doc corrected | **PR #119 merged** — `get_json` ratchet holds at 103 with a known floor |
| api-client: **every factory exported** (only `createCollectionsApi` was — the whole reason 16/17 modules went unused); member `playtime`/`wishlist`/`favorites`/`discoverPins`/`gameEditions`/`updates` on typed modules; client gaps closed (`all=1`, `resolveRequest`, favorites filters, `updates.scan/storeSearch/addWanted`, signals) | **PR #120 merged** |
| **CI flip:** `pytest-core` = `tests/ -m "not integration"` minus 31 named `--deselect`s. Measured **3,792 tests · 11m24s** bare, **16m12s with coverage · 0 failed · coverage 38% → 68%** | **PR #121** — green, merging |
| `LooseProps` counted by the `any` ratchet (206 sites; honest total **1,277**) | **PR #122** open |
| Coverage floor 35 → 65 (after #121) | next |
| Remaining: admin `adminApi` → typed modules; more member families (chat/social, notifications, ownership, systems…) as client modules land · button tail (109) + Modal/DataTable/showToast · `toast` de-dup · 13 mega-components · test files to TS, flip `tsconfig.base.json` · work the 1,277 down file by file | queued |

**D7 findings (refresh line missing) — for Phase 4, verified on the live box 2026-09-17:** the served `assets/base.js` carries `measureRefreshHz`; the standalone player page logs `Display refresh measured: 56.82Hz` in the in-app browser (throttled — the pane was hidden), so the code path works. Two real gaps: `standalone.html` loads `base.js?v=leave-guard-1` — a cache key never bumped for the refresh fixes, under `cache-control: max-age=3600` with no validator; and the sampler needs `requestAnimationFrame` ticks, which a hidden document never gets, so an iframe that boots while not visible measures nothing and logs nothing. Phase 4: bump the key on every `base.js` change (or derive it), sample on `visibilitychange` when hidden at load, and log *"waiting for a visible tab"* so silence stops being ambiguous. No service worker is involved (`pwa-sw.js` is a stub).

## D9 (2026-09-17, human) — run Phases 3–7 without waiting

"just continue phase 3-7 and continue to add to the human queue when needed, we will review all that at the end of p7." QA batches no longer gate phase boundaries: items go onto the Human Queue artifact as they arise, deploys still happen at phase ends (D2), and one review happens after Phase 7.

## Phase 4 — in progress (2026-09-17)

- Member engine choice (`v1/member-play-engine`): `user_preferences.browser_player_engine` + Alembic `c7d8e9f0a1b2`; `browser_player.resolve_engine()`; Preferences → *Play in browser* section; Admin → Emulators checkbox. Found and fixed on the way: the top-bar tile-size slider's save has been failing validation (absent SelectFields) since the slider shipped — partial saves now merge stored values.
- Human Queue: **p4-member-engine** — with EmulatorJS fetched and the admin box ticked, pick EmulatorJS in Preferences and press Play on a NES title; the EJS shell should open; untick the admin box and Play should return to WebRetro without touching Preferences.

## Incident 2026-09-17 — a commit reached `main` without a PR

`e6d5b4a4` (three Discover notes: article-row scroll lane, grab-to-scroll, Steam art fallback) was committed and pushed straight to `main`. Cause: a background job ("merge #124 then wait for #125/#126") ran `git checkout main` on this **shared** checkout while the foreground was mid-commit on `v1/discover-upcoming`, so the amend landed on `main` and `git push` went to `origin/main`. The change had passed every local gate (vitest 42 + 14, pytest 20, prettier, eslint, both JS ratchets), CI runs on `main` pushes and was queued immediately, and no force-push was used to hide it. If that run is red the commit is reverted through a PR.

**Rule from this:** on the shared checkout, **a background job never runs `git checkout`, `git switch` or `git pull`.** Merge/wait jobs poll and report; branch moves happen in the foreground only, immediately before the commit they serve. Same lesson as `43d95f15` in the 2026-09-02 session board, relearned.

## Riders from the capability harvest (2026-09-16)

A second landscape pass ran alongside Phase 1 (strategy register on the NAS:
`docs/strategy/capability-harvest-2026-09.md`, local-only by policy). Human ruling: **hybrid** —
three small substrate items ride into the phases that already plan the work above them;
everything structural is a **post-1.0 cycle** (`v1.1 capability harvest`, H1–H5) and is not
on this board. **Phase 2 takes no riders.** Each rider is one commit inside its host phase's PR.

| ID | Host | Rider | Why it belongs to the host, not beside it |
|---|---|---|---|
| R1 | Phase 4 | **Community achievement-set hash index** as a `set_completion.py` `VALID_SOURCES` entry (operator key, same opt-in shape as every keyed provider); persist the matched set id on the game record. Alembic revision. | Phase 4 owns the achievements surface (D4). It needs a ROM→set match; that match is a hash lookup `rom_hash.py` already computes, including inner archives. |
| R2 | Phase 4 | **`supports_achievements`** derived from R1; the badge and any CTA gate on it. | Pre-empts a known peer failure mode: matching ROMs against set entries that carry no achievements, so the UI promises what does not exist. Same discipline as `play_mode` / `play_blocker`. Browser sessions cannot unlock hardcore — Help copy says so. |
| R3 | Phase 6 | **VR ways-to-play rows** on `/ways-to-play` and game details — engine-scoped injector paths (Unreal 4.8–5.6 DX11/12; RE Engine) and a BYO runtime matrix (SteamVR, OpenXR runtime + OpenVR adapter, wireless streamers, wired link) — plus `vr_compat` (`native_vr` / `injector_profile` / `flat`) beside `is_vr`. | Phase 6 owns VR-L4b (detail CTAs) and has no data to render. **Hard boundary:** catalogue and deep-link only. Oneirodex never ships, injects or installs a DLL shim; upscaler tweaks are a runbook, not a file. |

Verification for R1/R2: `alembic upgrade head` + `alembic check` on a fresh DB; the new
source appears in `plugins.py` and `integrations_inventory.py` with a truthful status and
degrades to *no data*, never a silent `[]` (the F14 pattern). For R3: deploy, then read the
rendered rows — a source-only check is not evidence.

## Gates for every phase

Four ratchets (`api_envelope_lint`, `print_lint`, `get_json_lint`, `css-token-lint`) ·
scoped pytest + vitest · `alembic upgrade head` + `alembic check` when a model moves ·
`npm run lint` **and** `npm run format:check` · docs-sync · all 9 CI jobs green before merge.
