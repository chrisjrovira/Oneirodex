# v11 cycle — close 1.0.0, debt, the D10 remainders, the capability harvest

**Cut:** 2026-09-18 · **Base:** `main` @ `22a3f781` (v1 cycle closed, 27 PRs #113–#139) · **Ends at:** tagged `1.1.0`

Board for the post-1.0 program (**D12**: a new agent runs it from a written brief). Sibling of
[2026-09-16-v1-cycle.md](2026-09-16-v1-cycle.md); same shape — one branch per slice cut from
`main`, merged on all 9 CI jobs green, deploy + Reset Themes at phase ends, human QA accumulating
on the Human Queue artifact and reviewed per phase, never gating the next one.

## Standing decisions carried in (human — do not re-ask)

| # | Decision |
|---|---|
| D2 | Merge and deploy authority delegated; merge on fully green CI, rebuild Unraid + Reset Themes at phase ends. |
| D5 | Agent-harness files stay untracked — `CLAUDE.md`, `AGENTS.md`, `.claude/`, `.cursor/`, **and handoff briefs** (`docs/_private/`), CI-enforced by `tests/test_repo_hygiene.py`. |
| D9 | No QA-batch gating between phases; items accumulate on the Human Queue. |
| D10 | Tag 1.0.0 now; Phase 5/6 depth (themes, platform reach) is this program's H-T. |
| D11 | **Debt first**, before the feature tracks. |
| HV-1…4 | Capability harvest: hybrid slotting · cheats **A now, B gated** (B askable only once the L6 stance and INSP-35 both exist) · tiered register · all four tracks (H1–H4) in scope. |
| 2026-09-18 | **Tag:** fold `[Unreleased]` into `[1.0.0]`, generator note 36 → 39, tag at `main`. **PR #140:** the handoff brief moves to `docs/_private/`; only the test fix + hygiene guard merge. |

Hard constraints: no Discord · no Class A · no store downloads / DRM installs · never commit
unprompted · never overwrite the root `.env` · desktop code signing permanently out of scope.

Defaults taken where he did not answer (one word overturns either): `docker builder prune` on the
box is **not run** (68.6 GB reclaimable, `/var/lib/docker` at 93 %; re-raised only if a deploy fails
on disk) · cadence is **continuous with a report at each phase end**.

## Phases

| # | Phase | Branches | Status |
|---|---|---|---|
| T.0 | Dispose of PR #140 | `v1/handoff` | **done** — brief → `docs/_private/handoff-2026-09-18-post-1.0.md`; guard extended; PR retitled |
| T | Close 1.0.0 — fold, gates, tag, queue | `v11/release-close` | **done** — #141 merged; `v1.0.0` tagged at `d647dd3e`, GitHub release published 2026-09-18 |
| H-D | Pay the debt down | `v11/debt-*` | **done 2026-09-21** — H-D.1 #142/#144 · H-D.2 #145/#146 · H-D.3 #147/#148 · H-D.4 #149/#150 · H-D.5 #151/#163 (inventory + 2 ports); deployed 10:44, `/awake` 1.0.0, both React pages read live. The remaining 28 admin ports continue as the ratchet between later slices |
| H-T | D10 remainders — themes depth, platform reach | `v11/themes-*` · `v11/platform-*` | **done 2026-09-21** — T1 #166 · T2 #167 · T3 #168 · P1 #169 · P2 #170 · P3 #171 merged; P4 #172 merged; T4 AI backdrops is his (`p8-ai-backdrops`); deployed 2026-09-21 18:13 (GV 40, Reset Themes wrote 21 presets); #173 fixed the lazy-page module-identity bug found in the live check |
| H1 | Harvest — identify + metadata | `v11/h1-*` | **done 2026-09-22** — H1a #175 (GV 41) · H1b #176 · H1c #177 · H1d #178 · H1e #179 · H1f #180; INSP-7 / INSP-33 documented as deferred; deployed 2026-09-22 04:05 (ship log C:\od-modz\ship-0922-0355.log; /awake 1.0.0, 21 presets reset, anti-cheat feed 1167 titles on first boot), Reset Themes 40 → 41 |
| H2 | Harvest — mods URL → catalogue | `v11/h2-*` | **done 2026-09-22** — H2a #181 · H2b #182 · H2c #183 · H2d #184 · H2e #185; deployed 15:45. Live-found riders #196 (`can_edit` absent from the details payload) and #197 (Thunderstore communities paginate at 100) |
| H3 | Harvest — XR depth (after H-T platform) | `v11/h3-*` | **done 2026-09-22** — H3a #186 headset record (Alembic `a1b2c3d4e5f6`) · H3b #188 BYO runtime runbook |
| H4 | Harvest — stores, arcade, telemetry, assists | `v11/h4-*` | **done 2026-09-22** — H4a #189 Xbox/PSN (opt-in off, G5) · H4b #194 arcade (Alembic `b2c3d4e5f6a7`) · H4c #190 GPU · H4d #191 notify bus · H4e #192 save paths · H4f #193 overlay links; deployed 15:45 |
| H-I | The rest of the inventory, sized first | `v11/hi-*` | **sized 2026-09-22** — [2026-09-22-hi-sizing.md](2026-09-22-hi-sizing.md): 27 rows with size, dependency and slot; proposed order INSP-3 → INSP-4/21 → INSP-12 → a small-wins slice → INSP-9 → identify gaps, Jinja ports throughout. Five rows blocked on his answers |
| QA | Play-path fixes from his queue answer of 19 Sept | `v11/fix-*` | **done 2026-09-22** — #198 RAR5 / NeoGeo CD + `scripts/ops/playability_report.py` · #199 the play room could never save a state (no CSRF token) nor keep a whole one (exported mid-write); deployed 18:05, verified live. 40 platforms open bar Channel F, whose rows point at `.zip` paths the shelf no longer holds |
| DOCS | The README, the screenshots, the how-to videos | `v11/docs-overhaul` | **done 2026-09-22** — #200: README rebuilt (section banners and feature cards drawn from `od-tokens.css` and the shared rail icons, composited hero, screens gallery, video gallery) · 49 stills at 1600×900 across every member and admin surface · **20 narrated how-to clips** (edge-tts neural voice, burned-in captions, `.vtt` tracks, posters, transcripts) replacing the ten silent ones · five capture scripts, of which `serve_capture.py` stands a throwaway instance up in one command — which is what retired `p7-screens`, the queue row that had been waiting on his credentials since Phase 7. `main` had fixed the same `dist_asset` module-identity bug independently (`a63dfa5b`), so the duplicate commit was dropped in the merge. print-lint `EXEMPT` now covers the capture CLIs (stdout is their report), with tests pinning it. Deployed 2026-09-22 22:5x, `/awake` 1.0.0 |

## Measured on 2026-09-18 (the starting line, not July's numbers)

| | |
|---|---|
| Ratchets | api-envelope 11 · print 591 · get_json 103 · css-token 0 · any 1,276 |
| Components > 600 lines | **14**: `GameDetailsPage` 1,283 · `ChatPanel` 1,097 · `DupeGlance` 1,008 · `ArtStudioPage` 895 · `ImagesPage` 869 · `LibraryApp` 858 · `TrailersPage` 854 · `GameCard` 826 · `AccountModal` 809 · `LibrariesPanel` 741 · `OpsPage` 707 · `opsWidgets` 699 · `NewsPage` 696 · `EmulatorFirmwarePanel` 640 |
| Raw `className="od-btn…"` sites | 160 (loose grep) |
| Untyped tests | 174 `.test.js(x)` vs 14 typed; `tsconfig.base.json` `strict:false`; 5 local overrides |
| Backend > 1,000 lines | `routes_apis/scan.py` 1,769 · `updateschema.py` 1,700 (frozen, ADR 0004) · `cover_art_studio.py` 1,626 · `preset_themes.py` 1,405 · `software_identify.py` 1,403 · `scan_identify.py` 1,390 · `rom_archive.py` 1,227 · `scan_orchestration.py` 1,216 · `store_ownership.py` 1,113 · `routes_apis/game.py` 1,007 |
| Admin Jinja | 46 `admin*` templates |
| `toast` | three copies (shared, member, admin) |

## Phase T — closing 1.0.0 (2026-09-18)

- [x] `[Unreleased]` folded into `[1.0.0]`; upgrade note 36 → 39; `themes-reset.md` history rows 37/38/39. 340 bullets before and after.
- [x] Fresh-DB gate: `alembic upgrade head` → `d3e4f5a6b7c8`, `alembic check` clean (scratch DB `tagcheck_test`, dropped after).
- [x] Live gate on `:5006` (browser, computed styles): `/awake` 1.0.0 · `data-era="wood_den_80s"` · `.od-era-scene` 1280×720 at opacity 0.5 with `art/era/wood_den_80s.svg` served 200 · `resume_state` and `supports_achievements` on the browse payload · *Play in browser* section correctly absent (EmulatorJS not fetched) · *Install this library* row present but hidden (`isSecureContext` false on plain HTTP). All as designed; the two absences are `p7-emulatorjs-fetch` and `p7-tls`.
- [ ] **README media recapture (MISS-DOC-4 / MISS-QA-4) — open, needs him.** `scripts/capture_docs_media.py` logs in with `CAPTURE_USER`/`CAPTURE_PASS`; there is no live-box credential this side and there must not be. Human Queue row `p7-screens`. Does not gate the tag: docs media, not release code.
- [x] `main` run on `d647dd3e` green (run 35394233418) → annotated tag `v1.0.0` pushed; GitHub release from the `[1.0.0]` section (249 bullets).
- [x] Human Queue republished (v8): `p7-ssh` + `p7-tag` settled in the db, `p7-screens` + `p8-builder-prune` added, empty post-1.0 section opened.

## Gates for every slice

Five ratchets (`api_envelope_lint`, `print_lint`, `get_json_lint`, `css-token-lint`, `any_lint`) ·
scoped pytest + vitest · `alembic upgrade head` + `alembic check` when a model moves ·
`npm run lint` **and** `npm run format:check` · live verification for anything user-visible
(computed styles on `:5006`; compare child to parent, never to a typed-in viewport) · docs-sync ·
all 9 CI jobs green before merge · no direct pushes to `main`.

## Phase H-D — paying the debt down (2026-09-18 →)

### H-D.1 primitives — `v11/debt-primitives`
- `toast`: member and admin `utils/toast.ts` were byte-alike copies of `frontend/shared/src/toast.ts`; both are now re-export shims and the two app suites merged into one shared `toast.test.js`.
- **Found on the way:** the root `eslint.config.js` never matched `frontend/admin-app/**/*.{ts,tsx}` — the admin TS surface was unlinted since its conversion. Glob added; 0 errors, 3 dead bindings fixed, 2 `exhaustive-deps` warnings left for the `DashboardPage` / `OpsPage` decomposition.
- Button tail: 112 raw `od-btn` `<button>` sites → `<Button>` (100 by an AST codemod, 4 dynamic-className sites by hand; the other 8 hits were `.od-cbtn` buttons borrowing `od-btn--sm`, not this family). `Button` gained `secondary`/`quiet`/`lg`/`pill`. Ratchet `scripts/od_btn_lint.mjs` at **0**, in the admin-app-vitest CI job.
- `Modal` primitive (portal, Escape, backdrop, focus in/trap/restore, optional scroll lock) adopted by four dialogs; `DataTable` + `PM_IGNORE` moved to `@oneirodex/ui` with admin shims. `any` 1,266 → 1,265.

### H-D.2 mega-components — `v11/debt-decompose-*`
- `GameDetailsPage` 1,283 → 463: seven sections + three hooks under `pages/gameDetails/`, typed props (`detailsTypes.ts`). Existing 21 GameDetails tests unchanged and green; member suite 993. `any` 1,265 → 1,259.
- `ChatPanel` 1,097 → 497: four sections + two hooks + helpers under `components/chat/`, typed (`chatTypes.ts`). Chat tests 27 green. `any` −5.
- `DupeGlance` 1,008 → 597: sub-components + formatters to `components/dupeGlance/DupeCompare.tsx`, re-exported from the old path. 27 tests green. `any` −3.

### H-D.2 batch 2 — `v11/debt-decompose-2`
- `ArtStudioPage` 895→567 · `ImagesPage` 869→426 · `LibraryApp` 858→590 · `TrailersPage` 854→425 · `GameCard` 826→562 · `AccountModal` 809→147 · `LibrariesPanel` 741→433 · `OpsPage` 707→243 · `opsWidgets` 699→287 · `NewsPage` 696→323 · `EmulatorFirmwarePanel` 640→432. `any` 1,265 → 1,224 on this branch. Ratchet `scripts/component_size_lint.mjs` at 600, in CI (baseline holds only the three batch-1 files until #145 merges, then empty).
- **H-D.2 exit:** with #145 + this branch, no non-test `.tsx` exceeds 600 lines.
### H-D.3 typed tests + strict flip — `v11/debt-typed-tests`
- 173 test files → TypeScript, strict-clean; harness modules too. `tsconfig.base.json` `strict: true`, overrides removed; `checkJs` off (classic theme JS imported by admin tests). Real drift caught: `UpdatesInboxItem.uuid`, `EditionsResponse.tags`, the `testConventions` glob. Suites identical: 125 / 6 / 375 / 1,000 / 51.
- **Side effect + fix (#148):** the image build runs `tsc --noEmit` per SPA; with tests typed it reached `cssTokenLint.test.ts`, whose import of `scripts/css-token-lint.mjs` is outside the Docker build context, and the first rebuild failed. Each SPA now type-checks source only in the build (`tsconfig.build.json`; vitest still covers tests). Rebuilt 2026-09-19 14:52: `oneirodex-app` recreated on the new image, `/awake` 1.0.0, Reset Themes ran (default + 15 presets).
- **H-D.1–H-D.3 checkpoint:** `any` 1,276 → 1,208 (harness excluded) · `od_btn` 0 · `component_size` 0 · css-token 0. Backend ratchets unchanged (api-envelope 11 · print 591 · get_json 103).
### H-D.4 backend god-modules — `v11/debt-backend-<n>`, pure moves + facades, tests unchanged
- Method: `split_py.py` (scratchpad) cuts named line ranges into new modules verbatim, copies the import block, then iterates pyflakes to drop unused imports and add cross-module imports; asserts no non-import line was lost. Facade keeps re-exports for every externally imported name; test patch targets follow the function they patch.
- Batch 1: `routes_apis/scan.py` 1,769 → 370 (+ `scan_unmatched_rows` 361 · `scan_unmatched` 583 · `scan_unmatched_edit` 521) · `utils/cover_art_studio.py` 1,626 → 567 (+ `cover_art_tokens` 167 · `cover_art_paint` 683 · `cover_art_title` 280). `get_json` 103 re-homed (scan 2 / unmatched 2 / edit 7). Known-failure list unchanged (the three locally red tests are in `docs/dev/test-suite-failures-2026-09-16.md` and CI's `--deselect`).
- Batch 2 (`v11/debt-backend-2`): `software_identify.py` 1,403 → 736 (+ `software_identify_store` 213 · `software_identify_custom_game` 245 · `tgdb_platform_match` 266) · `preset_themes.py` 1,405 → 748 (+ `preset_themes_catalog` 467 · `preset_themes_geometry` 207) · `rom_archive.py` 1,227 → 660 (+ `rom_archive_types` 156 · `rom_archive_select` 146 · `rom_archive_zip` 323) · `store_ownership.py` 1,113 → 267 (+ `_common` 305 · `_gog` 210 · `_epic` 200 · `_amazon` 273) · `routes_apis/game.py` 1,007 → 385 (+ `game_batch` 646). Ratchets re-homed at identical totals (11 / 103 / 591). Eight test patch targets followed their functions. Splitter learned name-based blocks and dest→source cycle detection (the `CUSTOM_IGDB_BASE` cycle in software_identify was the lesson: a constant shared by the facade and a leaf lives in the leaf).
- **Deferred, with reason — not pure moves:** `services/scan_identify.py` 1,390 is one 860-line function (`retrieve_and_save_game`) plus collaborators that write a module `global settings`; `services/scan_orchestration.py` 1,216 is a 714-line `_scan_and_add_games_body` with a nested `process_single_game`, and tests patch its Flask collaborators on the module. Both need a behavioural refactor with the scan tests as the net, which is a slice of its own (parked on the inventory as INSP-SCAN-GODFN). `updateschema.py` 1,700 stays frozen (ADR 0004).
- **H-D.4 exit:** every backend module that can be split by moving is split; nothing above 1,000 lines remains except the three named above.
## Phase H1 — identify + metadata (2026-09-21 →)

Decision (plan): INSP-31 keyless hash-identify built; INSP-32 daily-DAT ingest documented as the fallback; INSP-33 offline index sized in H-I.

- **H1a `v11/h1-hash-identify-source`:** `utils/hash_identify.py` (`lookup_by_hashes` md5→sha1→crc via `safe_request`, generous parser, None on every failure, off under pytest unless `HASH_IDENTIFY_IN_TESTS=1`); wired as the fallback inside `set_completion.try_dat_hash_identify` after the local DAT miss (outer digest, then inner dumps) — `scan_identify.py` untouched; provider switch `hash_identify` (STORE_PROVIDER_IDS split from PROVIDER_IDS so Stage D is unchanged), plugin `provider.hash_identify`, inventory row; form checkbox + theme-JS handler → GV 41. Tests: `test_hash_identify.py` (12) + DAT identify + providers API + inventory = 54 passed.
- **H1b `v11/h1-dat-sources-tosec-mame` (#176):** `VALID_SOURCES` += tosec, mame; `parse_dat_xml(source=)` titles `machine` / `software` nodes by `<description>` for mame and reads nested `<part>/<dataarea>/<rom>`; TOSEC needed no peel change (verified). 6 new tests.
- **H1c `v11/h1-1g1r` (#177, stacked on #176):** `ReferenceSetEntry.parent_name` + Alembic `f1a2b3c4d5e6`; region-preferred dedupe at parse time; `preferred_entries` (parents only) drives completion; `clone_parent_for` + `clone_of` / `clone_of_owned_parent` advisory on the Duplicate glance. 5 new tests.

- **H1d `v11/h1-identity-agreement` (#178):** `IDENTIFY_AGREEMENT_MIN` (default 1 = today) — IGDB counts one source, each W34 agreeing catalogue row one more; at 2 an IGDB-only match goes to Review with `match_reason=insufficient_agreement` + a proposal sidecar carrying `sources_agreeing`. One block in `scan_identify` beside the `disagree` branch; helpers in `software_identify`. 6 tests.
- **H1e `v11/h1-anticheat-source` (#179):** `utils/anticheat_compat.py` reads the community anti-cheat list — one keyless fetch a day (`anticheat_poller`) into `static/library/anticheat/games.json`, lookups from the cache only (Steam id, then exact name on PC shelves); `game_card_flags.anticheat`; Details *Anti-cheat* fact (`AnticheatFact.tsx`, status chip + report link, worded as community reports); plugin `compat.anticheat` + inventory row. Gate (2) for cheats Option B now exists; nothing asks it. 9 + 2 tests.
- **H1f `v11/h1-dat-repair-dry-run` (#180):** `utils/set_repair.repair_preview` + `POST /api/reference-sets/repair-preview` (`validate_body`, `api_ok`): rename candidates · hash mismatches · clone-named (with `parent_owned`) · unknown, plus verified / unhashed counts; `mame` sets never propose renames. *Preview repairs* block on the reference-sets Jinja page (`static/js`, no Reset Themes). Report only. 5 tests.
- **Deferred, documented:** INSP-33 offline hash index and INSP-7 community ROM metadata — `docs/admin/libraries-and-scans.md` › *Identify sources deferred*, each with its trigger.
- **Phase close:** deployed 2026-09-22 04:05 (ship log C:\od-modz\ship-0922-0355.log; /awake 1.0.0, 21 presets reset, anti-cheat feed 1167 titles on first boot), Reset Themes (GV 41). Human Queue rows `p8-hash-identify`, `p8-1g1r`, `p8-anticheat`.

## Phase H2 — mods (2026-09-22 →)

Registries in API-quality order per the register: Thunderstore → Modrinth → GameBanana → Nexus (browse only, key he places) → CurseForge declined. All read-only + deep link; the only file movement stays the companion's BYO `source_url` staging.

- **H2a `v11/h2-loader-field` (#181):** `loader` on the mod row (open slug, `LOADERS` suggested) + pack `default_loader` (`PATCH /mods/pack`, bulk `PUT`); mods API bodies through `@validate_body` — three `get_json` sites retired, baseline **103 → 100**; desktop reads the loader into the apply result (*Needs BepInEx installed — not managed here*). No migration. 8 + 13 tests.
- **H2b `v11/h2-catalogue-thunderstore-modrinth` (#182, stacked on #181):** `utils/mod_catalog/` (`ModHit`; Thunderstore community → top-rated packages; Modrinth for Minecraft titles; `catalog_search` → `ok | unavailable (hits null) | unknown_source`); `GET /api/games/<uuid>/mods/catalog?source=` (librarian); member **Mods panel** (first member-side mods UI — list for all, default loader + add form + *Browse catalogue* drawer for librarians; *Add to list* records the registry page as `source_url`); plugins `mods.catalog.*`, inventory rows + CurseForge *declined*. 6 + 4 tests.

- **H2c `v11/h2-catalogue-gamebanana-nexus` (#183):** GameBanana (keyless, rate-limited — the game's mod feed filtered by name) and Nexus Mods (**browse only**, behind a personal `NEXUS_API_KEY` the operator places; its terms forbid third-party download automation). Without the key the Nexus tab says so rather than answering nothing; CurseForge carries a `declined` row with the ToS note.
- **H2d `v11/h2-profiles-export` (#184):** `utils/game_mod_profiles.py` — named sets of tracked mods per game; **Activate** is the one-click enable set the companion then stages; **Copy code** is `od-mod:` + base64url JSON (name, default loader, each mod's name / version / loader / source *page* — never a file). Import matches by id then source URL and lists the rest as *missing* instead of inventing rows. Six routes under `/api/games/<uuid>/mods/profiles`; JSON pack, no migration.
- **H2e `v11/h2-compat-gate-updates` (#185):** `requires` between rows (staged first, whatever the order); `loader_conflicts` on the mods GET drives a *Loader mismatch* notice and the companion refuses to apply until the rows and the pack default agree; the drawer marks tracked hits with **update: vX** and offers *Mark updated*; the companion refuses an empty or `Content-Length`-short download before writing it.
- **Phase close:** merged 2026-09-22, deployed 14:30 + 15:45 (no `GENERATOR_VERSION` move, so no Reset Themes). Live: four source tabs in the drawer, Integrations honest per registry (3 configured / Nexus *available* pending a key / CurseForge *declined*).
- **Found by reading it live, fixed after:** `can_edit` was never in the details payload (#196), so the librarian half of the Mods panel, the headset editor and the pre-existing cheats form were all gated on `undefined`; and Thunderstore's community list is paginated at 100, so reading page one meant Risk of Rain 2 / Valheim / Lethal Company always answered *no data* (#197).

## Phase H3 — XR depth (2026-09-22)

- **H3a `v11/h3-vr-profile-record` (#186):** `GameVrProfile` — one row per (game, kind): `native` / `injector` / `flat`, runtime `openxr` / `openvr`, the community **profile page** URL (http(s) only; files and paths refused) and a note. Alembic `a1b2c3d4e5f6`, re-headed onto the H1c chain before it opened. `vr_compat` derives from the rows when a librarian did not set it; `/vr` and its filters admit profile-backed titles; `GET|PUT|DELETE /api/games/<uuid>/vr_profiles[/<kind>]` with `validate_body`. **A record and a link — never a shim.**
- **H3b `v11/h3-runtime-matrix-runbook` (#188):** `docs/runbooks/vr-byo-runtime.md` — the four layers a headset seat needs around Oneirodex (OpenXR runtime, OpenVR adapter, streamer, tweaks), the wired USB-NCM link measured at 0.7 ms / ~756 Mbps, upscaler and foveation as documentation only, citing the maintained Linux VR wiki rather than restating it. Docs index, FAQ and README rows. Oneirodex ships, installs and injects none of it.

## Phase H4 — stores, arcade, telemetry, assists (2026-09-22)

- **H4a `v11/h4-store-xbox-psn` (#189):** `store_ownership_xbox.py` / `store_ownership_psn.py` on the per-store pattern; `VALID_STORES` += xbox, psn. **Live sync is off by default** — `ENABLE_UNOFFICIAL_STORE_SYNC` opts a store in (gate G5 honoured), the unofficial clients live in `requirements-optional.txt` and are imported lazily, so plugin and Integrations rows read *disabled* / *available* / *configured* truthfully. CSV import always works; a 401 fails closed in one sentence; the ownership summary carries `unofficial` / `opt_in` per store so the page can say *snapshot* honestly. IDs and names only — nothing downloads. Nintendo stays CSV.
- **H4b `v11/h4-arcade-launch-profile` (#194):** the arcade cores live in `ARCADE_COMPANION_CORES`, deliberately **not** in `platform_emulator_mapping` — ARCADE is a locked no-WASM platform and a mapped core is what would make a browser session look possible (`test_locked_wave_have_no_webretro_browser_keys` caught the first attempt). Choosing a core in Admin -> Emulators is what lifts the catalog-only lock. `Game.input_family` (Alembic `b2c3d4e5f6a7`) + `PATCH /api/games/<uuid>/input_family`; the companion appends the matching RetroArch input remap. No ROMs, no sets.
- **H4c `v11/h4-gpu-telemetry` (#190):** `system_stats.get_gpu_usage()` reads NVML when `pynvml` and a device exist, else a BYO reader at `TELEMETRY_READER_URL` (LibreHardwareMonitor / HWiNFO-class JSON, read-only through `safe_request`), else `None` — the tile then reads *n/a — no NVML or reader*, which is what the box shows. **Nothing bundled, no driver.** Touched functions moved `print()` -> `logger`; print baseline 591 -> 587.
- **H4d `v11/h4-notification-bus` (#191):** `utils/notification_bus.py` fans in-app alerts to an Apprise server and/or an ntfy topic through `safe_request`, riding the existing `admin_notify_*` flags; social kinds only with `NOTIFY_SOCIAL_TO_BUS=true` *and* the member's own preference. `POST /api/admin/notify-bus/test` says what was delivered. A dead endpoint is logged and never breaks the in-app notice.
- **H4e `v11/h4-save-paths-manifest` (#192):** `utils/save_paths.py` wraps the community save-location manifest (one keyless fetch a day, a compact index built beside it so no request parses the YAML); answers by Steam id then exact name, PC shelves only, `None` when silent. Details gains **Save location**, and **Open save folder** through the companion's existing `/path/open` contract — which refuses when a placeholder means nothing on that PC. The folder, not a backup.
- **H4f `v11/h4-assists-overlay` (#193):** `overlay_links` on an assist pack, validated as **http(s) pages only** (`@validate_body`), plus a PCGamingWiki deep link for every PC title even with no pack; rendered in the companion's stay-open Friends window for the game on screen. **No memory access, no injection, nothing read from the running process** — the condition the cheats ruling's Option A was cleared under. `get_json` baseline 100 -> 99.
- **Phase close:** deployed 2026-09-22 15:45. Live reads: Ops GPU tile *n/a — no NVML or reader*; Xbox/PSN *disabled* (opt-in off); notify bus *disabled* (nothing configured); a PC details page shows Save location with real manifest rows and the headset editor.

## Phase H-I — sizing (#195)

`docs/superpowers/plans/2026-09-22-hi-sizing.md`: one row per remaining inventory item with size, dependencies and a slot. Build order: INSP-3 -> INSP-29 -> INSP-4 (+21) -> INSP-4b -> INSP-12 -> a small-wins slice (SRV-4, GOW-3, LIGHT-3, ART-4) -> INSP-9 -> the identify gaps. The five native rewrites stay parked; admin Jinja ports continue between slices.

## Play-path fixes from the Human Queue (2026-09-22)

His `p7-resume` answer of **19 September** was not picked up until today — read back off the queue artifact mid-program. Three parts, all closed.

- **#198 `v11/fix-rar5-extraction` — NeoGeo CD / "the unrar tool error".** Four stacked faults, none of them a missing tool: the image's 7-Zip *lists* a RAR5 it cannot decompress (28 zero-byte files, *Unsupported Method*); `_extract_archive_via_cli` set `tool_name` from the listing step and never reached bsdtar, which decodes these archives; ROM names like `[!][NGCD-058]` are glob character classes to bsdtar, so an exact name finds nothing — fix: **backslash-escape the metacharacters**, since bsdtar honours them, which also stops it decoding members we did not ask for rather than unpacking 700 MB to reach one cue on a container with 1.2 GB of rootfs free; and one CRC-damaged member stops the whole run, so *Aero Fighters 2*'s bad track 12 of 34 hid the `.cue` at member **34 of 34** — fix: read the member bsdtar named, drop it, ask again for the rest, log it, bounded at six. Also stopped the rar path raising a generic *"Failed to read rar archive"* over the CLI's real diagnosis, the substitution that made this look like a missing unrar for months. Live: both NeoGeo CD titles resolve in ~15 s.
- **#199 `v11/fix-play-room-saves` — save states had never worked, for anyone.** `webretro.html` is served from `/static` and never rendered, so its `<meta name="csrf-token">` is permanently empty, and the CSRF cookie is `HttpOnly` — so **every upload and delete the room ever attempted was rejected `400 "The CSRF token is missing"`, silently.** The saves listing (the one call the room already makes at boot) now carries a token and the room remembers it; same exposure as the SPA's rendered meta — `@login_required`, same-origin, no CORS anywhere in the app. Second fault: RetroArch writes the 8-byte RZIP header at once and compresses the payload over 1–3 s while webretro copies the file into IndexedDB from its write callback, and `exportWithRetries` accepted the first non-empty read — measured live, **the first export of a 741-byte state returned 9 bytes**. It now prefers the file being written over the cached copy and retries until the size stops growing *and* exceeds any partial header. `?v=` cache key -> `save-states-4` (base.js / od-bridge.js are cached an hour with no validator — **bump it whenever either changes**).
- **`scripts/ops/playability_report.py` — his "run a test for each system".** Per platform: the promise (`play_mode_for_platform`), whether a mapped core is vendored, and **one real file opened through `resolve_playable_rom_path`**, the same call the play route makes. Read-only. Result on the live library: **40 platforms with games, 39 open cleanly** at three samples each. The single failure is data, not code — every Channel F row points at a `.zip` path while the shelf holds `.bin` files; a rescan of that library fixes it (offered on the queue, not run).
- **Phase close:** #199 merged 17:29, #198 17:50 (two CHANGELOG conflicts resolved by merging `origin/main`, never rebasing, keeping both sides' bullets). Deployed 18:05, `main` = `0befa9b4`, `/awake` healthy, `GENERATOR_VERSION` unchanged at 41. Re-verified against the shipped build rather than the test patches: *Sync now* stored 741 bytes from an empty list, and the report re-run reads `NEOGEO_CD ok`. Rows created while testing were deleted.

## Phase H-T — themes depth + platform reach (2026-09-21 →)

Plan corrections at the boundary: E2 already done, E4 half done, ART-6 `#stock` picker already exists (cover packs), `is_vr` is derived (no column), no device-list API, mods are JSON packs. Slices: T1 E1 console packs (GV 40) · T2 platformSkins → shared · T3 E4 per-era glyph set · T4 AI backdrops (his, `p8-ai-backdrops`) · P1 TC-4 device list · P2 TC-3 thin-seat audit · P3 `vr_compat` column · P4 VR ways-to-play rows. Phase end: deploy + Reset Themes once.

- **T1 `v11/themes-e1-console-packs`:** six `group: 'console'` presets (Cartridge red · Disc-tray blue · Signal green · Deep blue 16-bit · Woodgrain amber · Terminal teal), a `console` picker group between decade and cabinet, six geometry rows, six swatch rules in `form-components.css`, `GENERATOR_VERSION` 40. Theme tests 208 passed (`test_theme_eras` order assertion updated). Local env note: `pydantic-core` had drifted to 2.49.0 (repinned to 2.46.5 per `requirements.txt`).
- **T2 #167:** `platformSkins` table + `FAMILY_META` + `platformFamily()` in `@oneirodex/ui`; app wrappers keep their fallbacks; `any` 1,208 → 1,207; the `shared/TODO.md` entry resolved without an art decision.
- **T3 #168:** `railIconsEra.tsx` — eight destinations redrawn in three hands (pixel / rounded / angular), `RailIcon` prefers the room's hand on `era-*` / `console-*` themes only; shared 129 tests.
- **P1 #169:** `GET /admin/api/ops/devices` + `list_client_devices()`; Ops Companions panel renders the device table; FAQ / troubleshooting / thin-client rows.
- **P2 #170:** `seatCanUseCompanion()` + `ThinSeatNote`; six more surfaces stop offering companion actions on a thin seat; open-path copies instead of queuing.
- **P3 #171:** `Game.vr_compat` + Alembic `e5f6a7b8c9d0`; `game_card_flags` emits it; `?vr_compat=` filter; `PATCH /api/games/<uuid>/vr_compat` (`VrCompatBody`).
- **P4 #172:** Ways to play → In a headset (three deep-link rows), details line, VR badge title, `/vr` hub admits stored values + `?vr_compat=` strip; PSVR2-on-PC walk-through; README VR line.
- **T4:** AI backdrops need the SD.Next box — his (`p8-ai-backdrops`, gate G4). Human Queue v9 rows: `p8-console-packs`, `p8-era-glyphs`, `p8-ai-backdrops`, `p8-device-list`, `p8-vr-rows`, `p8-dependabot-majors`.
- **Local env:** persistent `oneirodextest` needs `alembic upgrade head` after a new migration (the harness `create_all`s a fresh DB only).

### H-D.5 admin Jinja→React — `v11/debt-admin-react-<n>`
- Inventory: `docs/dev/admin-jinja-inventory.md` (2026-09-19). 48 templates; 16 already React shells; **29 pages + 3 partials** still server-rendered (5,032 lines, 34 handler JS files). Tiers: T1 small panels (handler < 80 lines) → T2 forms/dashboards → T3 the `admin_manage_scanjobs` hybrid (986 + partials + 5 handlers).
- Port 1 (`v11/debt-admin-react-1`): `detail_layout` → `DetailLayoutPage` (pattern: flip `admin_render` to `spa`, delete the handler, same API, vitest beside the page, h1 = card title).
- Port 2 (`v11/debt-admin-react-2`): `emulator_profiles` → `EmulatorsPage`; the firmware / RetroAchievements / browser-pilot island folded in, `main.tsx` island mount removed. 28 pages + 3 partials remain.
- **H-D phase end (2026-09-21 10:44):** #151 + #163 merged, Unraid rebuilt, Reset Themes ran (GENERATOR_VERSION 39 unchanged), `/admin/detail_layout` and `/admin/emulator_profiles` verified live: `data-admin-render="spa"`, legacy body empty, no old handler script, 11 sections / 64 platform selects rendered by React. Box disk 93 % → 89 % (107 G free). Dependabot opened #152–#162 (vite 6→8, vitest 3→5, python group) — his call, not merged.
- The tail is a program, not a slice: each further port is its own PR on the D9 cadence. H-D ends with this port deployed; the T1–T3 ports continue between H-T / H1–H4 slices as the phase's ratchet (the count on the inventory goes down, never up).

### DOCS — the README pass (2026-09-22, #200)

The front page was a wall of text with four screenshots from 1 September and ten silent clips. It
is now generated: `render_readme_art.py` draws the banners, feature cards and poster tiles from the
theme's own tokens and the product's rail icons, so the page tracks the app instead of
approximating it, and `capture_docs_media.py` / `capture_howto_videos.py` shoot every surface and
every feature against a live instance.

- **`serve_capture.py` is the slice that mattered.** One command: scratch DB, gitignored env with
  every outbound integration off, admin plus a second member, the five legal sample ROMs, Art-studio
  covers, two-voice chat history, uvicorn. That is why the docs no longer need an account on his box,
  and why `p7-screens` came off the Human Queue instead of being answered.
- **The honesty rules are enforced, not documented.** A still whose page errors, comes up empty or
  loads *unstyled* is skipped and the file left alone (exit 3); a clip whose subject step the UI
  cannot perform writes no file at all; an optional step whose affordance is missing is dropped from
  picture *and* narration. What the sample data cannot show — browser play pressed, a LiveKit
  session, keyed metadata — is listed in `howto/README.md` rather than staged.
- **Ratchet:** the capture CLIs are `EXEMPT` in `print_lint.py` for the reason `updateschema.py` is
  (stdout is the interface), not re-baselined upward; their dead baseline entries are gone and two
  tests pin both facts.
- **Open with him:** `p10-readme-voice` (the voice was my pick; one env var to change),
  `p10-clip-gaps` (re-record the three undemonstrable sections against his box? — a privacy call),
  `p10-future-slate` (the nine features of pass 9).

