---
description: >-
  @agent-team — Index of GameTheca multi-agent roles and how to run a wave.
  Attach when starting multi-role work or choosing an owner.
alwaysApply: false
---

# Agent team

Follow `.cursor/skills/agent-team/SKILL.md`.

Wave: `@agent-pm` → (`@agent-gamemaster` if platforms/ROM/DAT/metadata) → (`@agent-ops` if Unraid/Compose/health) → implementers (`uiux` / `backend` / `desktop` / `ops`) → `@agent-qa` → `@agent-docs` as needed.

Locks: OIDC opt-in; no Discord; no romhacking.net scrape; commit only when asked.
