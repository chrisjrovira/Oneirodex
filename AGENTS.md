# GameTheca agent entry

Canonical product and engineering defaults: [docs/dev/agent-locks.md](docs/dev/agent-locks.md).  
Skills, domain seats, and context tiers: [docs/dev/agent-skills.md](docs/dev/agent-skills.md).

Edit skills and agents under `.cursor/` only. `.claude/` is a generated mirror (`python scripts/sync_prompt_trees.py`). Cursor glob rules live in `.cursor/rules/` and are not copied.

Always-on layout, tests, and ratchets: [CLAUDE.md](CLAUDE.md).
